name: secret_diary
description: "손글씨로 쓰는 비밀 일기 — AI가 읽고 답장하는 감성 일기장"
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  characters: ^1.4.0
  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.6.1
  google_mlkit_digital_ink_recognition: ^0.14.1
  perfect_freehand: ^2.3.2
  drift: ^2.26.0
  drift_flutter: ^0.2.4
  shared_preferences: ^2.5.3
  google_fonts: ^6.2.1
  http: ^1.3.0
  intl: ^0.20.2
  uuid: ^4.5.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  fake_async: ^1.3.2
  shared_preferences_platform_interface: ^2.4.1
  drift_dev: ^2.26.0
  build_runner: ^2.4.15

flutter:
  uses-material-design: true
